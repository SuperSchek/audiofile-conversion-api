{
  "name": "ffmpeg-api",
  "version": "1.0.0",
  "description": "REST API that converts .m4a and .mp3 files to .mp3 128kbps using ffmpeg. Built for deployment to Heroku.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sander Schekman",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^5.0.0",
    "nodemon": "^1.14.12"
  },
  "dependencies": {
    "aws-sdk": "^2.190.0",
    "express": "^4.16.2",
    "express-fileupload": "^0.4.0",
    "fluent-ffmpeg": "^2.1.2"
  }
}
